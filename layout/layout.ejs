<!DOCTYPE html>
<html lang="<%= (page.language ? page.language : hexo.config.plugins['hexo-multilang'].languages[0]) %>">
<head>
    <%- partial('_partial/head',{cache: true}) %>
</head>
<body>
    <div class="wrapper">
        <%- partial('_partial/header',{cache: true}) %>
        <div class="main">
            <%- body %>
        </div>
        <%- partial('_partial/footer',{cache: true}) %>
    </div>
</body>
</html>
